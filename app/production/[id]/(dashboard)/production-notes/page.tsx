/**
 * Production Notes Page (Production Module)
 *
 * Renders the production notes interface for a specific production from Supabase
 */
import ProductionNotesPage from '@/app/(dashboard)/production-notes/page'

export default function ProductionProductionNotesPage() {
  return <ProductionNotesPage />
}
