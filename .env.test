# Test Environment Configuration for Chrome DevTools MCP

# Development Mode
NEXT_PUBLIC_DEV_MODE=true

# Chrome DevTools MCP Configuration
# Enable Chrome DevTools MCP for advanced browser testing
ENABLE_CHROME_DEVTOOLS_MCP=true

# Chrome DevTools MCP Options
CHROME_DEVTOOLS_HEADLESS=true
CHROME_DEVTOOLS_ISOLATED=true
CHROME_DEVTOOLS_TIMEOUT=30000
CHROME_DEVTOOLS_DEBUG=false

# Test-specific Supabase (mock)
NEXT_PUBLIC_SUPABASE_URL=mock_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=mock_supabase_key