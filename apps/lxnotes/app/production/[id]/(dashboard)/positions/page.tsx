/**
 * Production Positions Page
 *
 * Renders the positions management interface for a specific production from Supabase
 */
import PositionsPage from '@/app/(dashboard)/positions/positions-impl'

export default function ProductionPositionsPage() {
  return <PositionsPage />
}
