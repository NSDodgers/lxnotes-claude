/**
 * Production Settings Page
 *
 * Renders the settings interface for a specific production from Supabase
 */
import SettingsPage from '@/app/(dashboard)/settings/page'

export default function ProductionSettingsPage() {
  return <SettingsPage />
}
