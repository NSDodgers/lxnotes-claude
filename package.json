{
  "name": "lxnotes-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.9.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test:e2e": "turbo run test:e2e",
    "audit:security": "npm audit --omit=dev --audit-level=high"
  },
  "devDependencies": {
    "turbo": "^2.8.11"
  },
  "dependencies": {
    "tailwindcss": "^4.1.18"
  }
}
